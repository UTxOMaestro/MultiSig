# Blockfrost (required)
BLOCKFROST_PROJECT_ID=YOUR_MAINNET_BLOCKFROST_KEY

# NOTE:
# - The multisig native script + allowed signers are hardcoded in `server.js` (as requested).
# - Network is also hardcoded to mainnet in `server.js`.

# CORS / Server
ALLOWED_ORIGIN=*
PORT=3000
